SELECT course.course_code, instructor.first_name AS Instructor_first_name, instructor.last_name AS Instructor_last_name, sum(num_credits) FROM student_schedule JOIN course, instructor WHERE course.course_id = student_schedule.course_id AND student_schedule.student_id = 1 AND course.instructor_id = instructor.instructor_id GROUP BY course.course_id;
